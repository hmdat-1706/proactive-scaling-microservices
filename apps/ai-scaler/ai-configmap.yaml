apiVersion: v1
kind: ConfigMap
metadata:
  name: ai-scaler-config
  namespace: boutique
data:
  TZ: "Asia/Ho_Chi_Minh"
  MLFLOW_TRACKING_URI: "file:///mlflow/artifacts"
  PROMETHEUS_URL: "http://monitoring-stack-kube-prom-prometheus.monitoring.svc.cluster.local:9090/api/v1/query_range"
  PROMETHEUS_QUERY: "sum(rate(traefik_service_requests_total[1m]))"
  DATASET_PATH: "test_dataset.csv" 
  PREDICT_MINUTES: "15"
  DATA_INTERVAL: "5"
